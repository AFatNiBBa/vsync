{
  "private": true,
  "scripts": {
    "build": "npm run build -w package/vsync",
    "start": "npm run start -w package/vsync",
    "icon": "npm run icon -w package/vsync",
    "docker": "docker build --tag vsync . && docker stop vsync && sh ./docker.sh",
    "style": "npm run start -w package/style",
    "clean": "git clean -Xdf"
  },
  "workspaces": [
    "package/*"
  ]
}
